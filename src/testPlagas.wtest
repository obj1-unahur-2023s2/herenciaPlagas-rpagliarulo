import plagas.*

describe "Plaga de cucarachas" {

	test "Nivel de daño es 10" {
		
		const cuca1 = new PlagaDeCucarachas(poblacion= 20, pesoPromedio= 10)
		assert.equals(10, cuca1.nivelDeDanio())

	}
	
	test "Transmite enfermedades" {
		
		const cuca1 = new PlagaDeCucarachas(poblacion= 20, pesoPromedio= 10)
		assert.equals(true, cuca1.transmiteEnfermedades())

	}
}